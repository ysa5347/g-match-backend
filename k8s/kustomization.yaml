apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: g-match

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.yaml
  - mysql.yaml
  - redis.yaml
  - django.yaml

# Common labels
commonLabels:
  project: g-match
  managed-by: kustomize

# Images (can be overridden per environment)
images:
  - name: YOUR_REGISTRY/g-match-backend
    newName: YOUR_REGISTRY/g-match-backend
    newTag: latest
